spring:

  application:
    name: ${APPLICATION_NAME:online-dictionary}

  datasource:
    url: ${DB_URL:jdbc:postgresql://${DB_HOST}:5432/online-dictionary?currentSchema=${spring.application.name}}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

logging:
  file:
    name: online-dictionary.log
